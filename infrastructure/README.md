# Infrastructure

Infrastructure as Code for the platform.

```
infrastructure/
├── docker/      # Dockerfiles and compose overrides
├── aws/         # ECS, IAM, SQS, CloudWatch (future)
└── terraform/   # Terraform modules and environments
```
