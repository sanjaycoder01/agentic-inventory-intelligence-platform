{
  "name": "@aiip/api-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Public API entry point — validates and routes requests to services",
  "scripts": {
    "dev": "echo 'TODO: start api gateway'",
    "build": "echo 'TODO: build api gateway'",
    "lint": "echo 'TODO: lint api gateway'",
    "test": "echo 'TODO: test api gateway'"
  }
}
