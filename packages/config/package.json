{
  "name": "@aiip/config",
  "version": "0.1.0",
  "private": true,
  "description": "Environment variables, AWS and MongoDB configuration",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'TODO: build config package'",
    "lint": "echo 'TODO: lint config package'",
    "test": "echo 'TODO: test config package'"
  }
}
